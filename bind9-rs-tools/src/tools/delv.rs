//! `delv` — BIND delv tool (addendum §2).
//!
//! Status: ARCHAEOLOGY — the tool is inventoried in the utility manifest
//! ([`crate::historical::manifest`]) and its surface is archived; the
//! implementation lands in dependency order (§64).  Every CLI surface gets
//! its own courts (addendum §15): CLI, input parsing, stdout, stderr, exit
//! status, environment, filesystem, network, wire, timeouts, signals, TTY,
//! interactive mode, historical versions, security regressions.
