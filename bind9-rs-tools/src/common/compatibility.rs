//! Compatibility profiles for the tools (spec §58): the exact BIND line and
//! maintenance release whose observable behavior each tool reproduces, and
//! the switch points between profiles.  The version-delta database drives
//! which profiles exist; no profile is claimed without evidence.
//!
//! Status: ARCHAEOLOGY — `common::versioning` carries the current target.
