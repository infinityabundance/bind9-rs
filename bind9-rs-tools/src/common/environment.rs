//! Environment interaction: locale-sensitive behavior, `TZ`, `HOME`
//! (`~/.digrc`), environment variables BIND tools observe (§32).
//!
//! Status: ARCHAEOLOGY — the dig `when_text` local-date path (time module)
//! is the first courted consumer.
