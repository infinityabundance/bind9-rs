schema_version = 1
court_id = "RENDER-COMPRESS-0004"
question = "dns_name_towire with DNS_COMPRESS_LARGE: 1024-slot table (AXFR/update semantics)"
subsystem = "dns/compress"
oracle_versions = ["9.20.26"]
harness = "probe"
normalization = []
invariants = [
    "names after the small table's 75% load cap (48 of 64 slots) still compress with the large table",
    "cumulative message hex must match byte-for-byte",
]
timeout_secs = 60
nondeterminism_policy = "none"
security_classification = "none"
references = [
    "lib/dns/include/dns/compress.h: DNS_COMPRESS_SMALLBITS=6, DNS_COMPRESS_LARGEBITS=10",
    "lib/ns/xfrout.c: AXFR/IXFR responses use DNS_COMPRESS_CASE | DNS_COMPRESS_LARGE",
    "lib/dns/request.c: update requests add DNS_COMPRESS_LARGE",
    "spec §16",
]
