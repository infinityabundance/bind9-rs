schema_version = 1
court_id = "RENDER-COMPRESS-0002"
question = "dns_name_towire with DNS_COMPRESS_DISABLED: no table updates, no pointers"
subsystem = "dns/compress"
oracle_versions = ["9.20.26"]
harness = "probe"
normalization = []
invariants = [
    "every name renders in full, regardless of prior names",
    "cumulative message hex must match byte-for-byte",
]
timeout_secs = 60
nondeterminism_policy = "none"
security_classification = "none"
references = [
    "lib/dns/compress.c dns_compress_name: early return when DNS_COMPRESS_DISABLED (9.20.26)",
    "lib/ns/client.c: named sets DNS_COMPRESS_DISABLED when the view's message-compression is off",
    "spec §16",
]
